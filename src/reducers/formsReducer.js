import {} from '../actions';

// Set initial state items for form elements
export const initialState = {};

export default (state = initialState, action) => {
  
  // Action handlers
  
  
  return state;
  
};