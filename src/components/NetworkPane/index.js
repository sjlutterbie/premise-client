import React from 'react';
import './NetworkPane.css'

// <NetworkPane /> presents the "Network View", where the overall story network
//  is presented as a network diagram, and via which the 'network actions' are
//  possible.

export default function NetworkPane(props) {
  
  return (
    <div className="network-pane">
      <p>Network Pane</p>
    </div>
  );
  
}